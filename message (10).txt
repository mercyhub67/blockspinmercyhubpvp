print("Loading Function")

local game = game
if typeof(game) ~= "Instance" then game = workspace.Parent end

Players = game.Players
repeat 
	Client = Players.LocalPlayer
	wait()
until Client

local Char = Client.Character
local Humanoid = Char.Humanoid
local Root = Char.HumanoidRootPart

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local PathfindingService = game:GetService("PathfindingService")
local CollectionService = game:GetService("CollectionService")

do
	Writefile = writefile
	Readfile = readfile
	Isfile = isfile
	Isfolder = isfolder
	Makefolder = makefolder
	Delfile = delfile
end

do
	FileName = tostring(Client.UserId).."_Settings.json"
	BaseFolder = "Mystic Hub"
	SubFolder = "Blox Spin"
end

local Config = {}

function SaveSetting() 
	local json = HttpService:JSONEncode(Config)

	if not Isfolder(BaseFolder) then
		Makefolder(BaseFolder)
	end
	if not Isfolder(BaseFolder.."/"..SubFolder) then
		Makefolder(BaseFolder.."/"..SubFolder)
	end

	Writefile(BaseFolder.."/"..SubFolder.."/"..FileName, json)
end

function LoadSetting()
	if Isfile(BaseFolder.."/"..SubFolder.."/"..FileName) then
		Config = HttpService:JSONDecode(Readfile(BaseFolder.."/"..SubFolder.."/"..FileName))
	end
end

LoadSetting()

----------------------------------------------------------------------------------------------------------------------
local CoreNet = require(ReplicatedStorage.Modules.Core.Net)
local CoreData = require(ReplicatedStorage.Modules.Core.Data)
local DoorClient = require(ReplicatedStorage.Modules.Game.DoorClient) 
local ModuleATM = require(ReplicatedStorage.Modules.Game.ATM.ATM)
local CoreJob = require(ReplicatedStorage.Modules.Game.Jobs.JobData)

local ModuleService = {}
ModuleService.__index = ModuleService

function ModuleService.new()
	return setmetatable({
		[1] = {
			Hookfunction = hookfunction,
			Clonefunction = clonefunction,
			FireProximityPrompt = fireproximityprompt,
			Islclosure = islclosure
		},
		[2] = {}
	}, ModuleService)
end

for _, Tags in CollectionService:GetAllTags() do
	if not Tags:find("Door") then continue end
	for _, v in CollectionService:GetTagged(Tags) do
		for _, v1 in v:GetDescendants() do
			if not v1:IsA("BasePart") or v1:FindFirstChild("Modifier") then continue end
			
			local v_ = Instance.new("PathfindingModifier", v1) do
				v_.Name = "Modifier"
				v_.PassThrough = true
				v_.Label = "Noclip"
			end
		end
	end
end

function ModuleService:Error(Class)
	for _, v in pairs(ModuleATM.class.objects) do
		if v.instance == Class then
			local isDisabled = v.states.disabled:get()
			return isDisabled
		end
	end
	return nil
end

function ModuleService:Thread()

end

function ModuleService:Bypass()
	local Success, Error = pcall(function()
		local Security = debug.getupvalue(CoreNet.send, 2)
		local Method = debug.getinfo(Security).what == "Lua"

		if not Method then error("Bypass Failed.") return end
		self[1].Hookfunction(
			Security,
			function(f, ...)
				return f(...)
			end
		)
	end)

	if not Success then error(Error) return end
	
	local Core = {
		Send = self[1].Clonefunction(CoreNet.send),
		Get = self[1].Clonefunction(CoreNet.get),
	}
	
	self[2].Send = function (...)
		local FirstArg = select(1, ...)
		if type(FirstArg) ~= "string" then
			warn("First argument is not a string. Network call blocked.")
			return
		end

		return Core.Send(...)
	end

	self[2].Get = function (...)
		local FirstArg = select(1, ...)
		if type(FirstArg) ~= "string" then
			warn("First argument is not a string. Network call blocked.")
			return
		end

		return Core.Get(...)
	end
end

function ModuleService:Closest(Instances, Condition, Target)
	local Condition = Condition or function() return true end
	local ClosestTarget = nil
	local ShortestDistance = math.huge
	
	
	for i, v in pairs(Instances:GetChildren()) do
		if Condition(v) then
			local Distance = (Target and Target or Root.Position - v.Position).Magnitude
			if Distance < ShortestDistance then
				ClosestTarget = v
				ShortestDistance = Distance
			end
		end
	end
	
	return ClosestTarget
end

function ModuleService:Detected()
	local Notification = Client.PlayerGui:FindFirstChild('Notifications')
	for _, v in pairs(Notification.Frame:GetChildren()) do
		if v.Name == "Notification" and (v.Text == "Teleport detected") then
			return true
		end
	end
	return nil
end

function ModuleService:Dist(Target)
	return (Root.Position - Target).Magnitude
end

local IsTeleporting = false
function ModuleService:Teleport(Destination)
	if IsTeleporting then return end
	IsTeleporting = true
	local Path = PathfindingService:CreatePath({
		AgentCanJump = true,
		AgentHeight = 3.8,
		AgentRadius = 2,
		AgentMaxSlope = 90,
		WaypointSpacing = 3,
		AgentJumpRadius = 50,
		AgentJumpHeight = 60,
		AgentCanClimb = true,
		AgentClimbHeight = 60,
		Costs = {
			DoorArea = 0;
			BlockedNode = 50;
		}
	})

	local Success, ErrorMessage = pcall(function()
		Path:ComputeAsync(Root.Position, Destination.CFrame)
	end)

	if not Success or Path.Status ~= Enum.PathStatus.Success then
		return
	end

	local Waypoints = Path:GetWaypoints()
	local BaseSpeed = 37

	for _, Waypoint in ipairs(Waypoints) do
		local OffsetY = (Waypoint.Action == Enum.PathWaypointAction.Jump) and 11 or 2.515
		local TargetPosition = Waypoint.Position + Vector3.new(0, OffsetY, 0)

		local StartPosition = Root.Position
		local DirectionVector = TargetPosition - StartPosition
		local Distance = DirectionVector.Magnitude

		if Distance < 0.1 then
			continue
		end

		local Direction = DirectionVector.Unit
		local StartTime = tick()
		local MovedDistance = 0

		while MovedDistance < Distance do
			if self:Detected() then
				continue
			end

			if Humanoid.Health <= 0 then
				continue
			end

			local ElapsedTime = tick() - StartTime
			MovedDistance = math.min(ElapsedTime * BaseSpeed, Distance)

			local NewPosition = StartPosition + Direction * MovedDistance

			if Humanoid.Sit then
				Humanoid.Sit = false
			end

			self[2].Send("set_sprinting_1", true)
			Root:PivotTo(CFrame.new(NewPosition))
			task.wait()
		end
	end

	IsTeleporting = false
	return true
end

function ModuleService:Get_Skill_Level(ClassName)
	local SkillsScrollingFrame = Client.PlayerGui.Skills.SkillsHolder.SkillsScrollingFrame
	for _, Child in pairs(SkillsScrollingFrame:GetChildren()) do
		if not Child:IsA("ImageButton") then continue end
		local Title = Child:FindFirstChild("SkillTitle")
		if Title then
			local Name, Level = string.match(Title.Text, "%s*([^:]-)%s*:%s*(%d+)")
			if Name and string.lower(Name) == string.lower(ClassName) then
				return tonumber(Level) or 0
			end
		end
	end
	return 0
end

function ModuleService:BurgePlace()
	local BurgePlaceBeacon = workspace:FindFirstChild("BurgePlaceBeacon")
	
	if Client:GetAttribute("Job") ~= "janitor" then
		if self:Dist(BurgePlaceBeacon:GetPivot().Position) > 10 then
			self:Teleport({ 
				CFrame = BurgePlaceBeacon:GetPivot().Position 
			})
			return
		end
		self[2].Send("apply_for_job", BurgePlaceBeacon)
		return
	end
	
	local ATM = self:Closest(workspace.Map.Props.ATMs, nil, BurgePlaceBeacon:GetPivot().Position)
	
	if CoreData.money.hand >= 100 and not self:Error(ATM) then
		local Position = ATM:GetPivot().Position
		self:Teleport({
			CFrame = Position
		})
		self[2].Get("transfer_funds", "hand", "bank", CoreData.money.hand)
		return
	end
	
	local Item = Client.Backpack:FindFirstChild("Mop") or Char:FindFirstChild("Mop")
	if Item.Parent == Client.Backpack then
		Item.Parent = Char
	end
	
	local Level = self:Get_Skill_Level("Janitor")
	local Interior = workspace.Map.Tiles.BurgerPlaceTile.BurgerPlace:FindFirstChild("Interior")
	local Puddle = self:Closest(Interior:FindFirstChild("Puddles"), function(v)
		return v.Transparency ~= 1 and 
			((v.Name == "LargePuddle" or v.Name == "SmallPuddle") and Level >= 5) or 
			(v.Name == "OilPuddle") and Level >= 35 or
			(v.Name == "ToxicPuddle" and Level >= 50) or 
			(v.Name == "NastyPuddle" and Level >= 35)
	end)
	
	if not Puddle then return end
	repeat
		if Client.Backpack:FindFirstChild("Mop") then
			Client.Backpack:FindFirstChild("Mop").Parent = Char
		end
		
		if Puddle.Transparency >= 0.99 then
			break
		end
		
		local PuddlePosition = Puddle:GetPivot().Position
		self:Teleport({ 
			CFrame = PuddlePosition 
		})
		
		self[2].Send("start_cleaning_puddle", Puddle)
		Puddle:GetPropertyChangedSignal("Transparency"):Wait()
	until Puddle.Transparency >= 0.99 or Humanoid.Health <= 0
end

function ModuleService:Quick_11()
	local TypeBox
	local Quick11 = workspace.Map.Tiles.GasStationTile:FindFirstChild("Quick11").Interior
	local Quick11_ATM = Quick11:FindFirstChild("ATM")
	local Quick11Beacon = Quick11:FindFirstChild("Quick11Beacon")
	local Quick11_Door = Quick11.DoorSystem
	
	if Client:GetAttribute("Job") ~= "shelf_stocker" then
		if self:Dist(Quick11_Door:GetPivot().Position) < 150 then
			self[2].Send("apply_for_job", Quick11Beacon)
		end
		self:Teleport({ 
			CFrame = Quick11_Door:GetPivot().Position 
		})
		return
	end
	
	if CoreData.money.hand >= 100 and not self:Error(Quick11_ATM) then
		local Position = Vector3.new(103, 255, 168)
		self:Teleport({
			CFrame = Position
		})
		self[2].Get("transfer_funds", "hand", "bank", CoreData.money.hand)
		return
	end

	local ShelfStock = Quick11.ShelfStockingJob
	local L_Box = ShelfStock:FindFirstChild("GoldenBox")
	local L_Box_Text = L_Box.BillboardGui.Frame.StockLeft.Text

	if tonumber(L_Box_Text:match("(%d+)/")) > 0 then
		TypeBox = "GoldenBox"
	else
		TypeBox = "NormalBox"
	end

	local CanNoclip = Vector3.new(98.5302963256836, 257.29205322265625, 205.9313201904297)
	local BoxTool = Client.Backpack:FindFirstChild("BoxTool") or Char:FindFirstChild("BoxTool")
	if not BoxTool then	
		if TypeBox == "GoldenBox" then
			local GoldenBoxPart = ShelfStock:FindFirstChild(TypeBox)
			local BoxPosition = GoldenBoxPart.Position

			self:Teleport({ 
				CFrame = CanNoclip
			})
			Root.CFrame = CFrame.new(CanNoclip)
			task.wait(.5)
			Root.CFrame = CFrame.new(CanNoclip - Vector3.yAxis * 20)
			task.wait(.5)

			self:Teleport({ 
				CFrame = BoxPosition
			})
			for i = 1, 10 do
				if GoldenBoxPart:FindFirstChild("ProximityPrompt") then
					self[1].FireProximityPrompt(GoldenBoxPart.ProximityPrompt)
				end
			end

			self:Teleport({ 
				CFrame = CanNoclip - Vector3.yAxis * 20
			})

			Root.CFrame = CFrame.new(CanNoclip - Vector3.yAxis * 20)
			task.wait(.5)
			Root.CFrame = CFrame.new(CanNoclip)
			task.wait(.5)
			return
		end

		if self:Dist(Quick11_Door:GetPivot().Position) > 20 then
			self:Teleport({ 
				CFrame = Quick11_Door:GetPivot().Position  
			})
			return
		end
		
		for i = 1, 8 do
			self[1].FireProximityPrompt(ShelfStock:FindFirstChild(TypeBox).ProximityPrompt)
		end
		return
	end

	local StandAttachment
	local StartCash = CoreData.money.hand
	for _, v in pairs(ShelfStock.Shelves:GetChildren()) do
		if v:FindFirstChild("Attachment") then
			StandAttachment = v.Position
			break
		end
	end
	
	if not StandAttachment then return end
	repeat
		if not BoxTool then break end
		if not Char:FindFirstChild("BoxTool") then
			Humanoid:EquipTool(BoxTool)
		end 
		if (Root.Position - StandAttachment).Magnitude > 2 then
			self:Teleport({
				CFrame = StandAttachment
			})
		end
		RunService.Heartbeat:Wait()
	until CoreData.money.hand > StartCash or Humanoid.Health <= 0 or not BoxTool
end

local InstanceService = ModuleService.new() do
	InstanceService:Thread()
	InstanceService:Bypass()
end

task.spawn(function()
	while task.wait() do
		local Success, Error = pcall(function()
			if Config["Auto Janitor"] then
				InstanceService:BurgePlace()
			elseif Config["Auto Quick 11"] then
				InstanceService:Quick_11()
			end
		end)
		if not Success then warn("Error:", Error) end
	end
end)

---------------------------------------------------------------------
do
	local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
	local ThemeName = "Dark"

	local Window = Library:CreateWindow({
		Title = "Mercy hub",
		Author = "by Golfeiei",
		Icon = "rbxassetid://73712053070820",
		Theme = ThemeName,
		HideSearchBar = false,
		Transparent = true,
		ToggleKey = Enum.KeyCode.G,
		Resizable = true,
	})
	
	local Tag = Window:Tag({
		Title = "Mystic Farm🌾",
		Color = "Text",
	})

	
	local Default = Window:Tab({
		Title = "AUTO FARM🌾",
		Icon = "tractor",
		--IconColor = Purple,
		IconShape = nil,
		Border = true,
	})
	
	function Toggle(Section, Title, Desc, Value)
		local Args = {} do
			Args.Title = Title
			Args.Desc = Desc
			Args.Value = Value
			Args.Callback = function(v) 
				Config[Title] = v
				SaveSetting()
			end
		end
		return Section:Toggle(Args)
	end

	Toggle(Default, "Auto Janitor", "Automatically picks up or clears nearby dropped items.", false)
	Default:Space()
	Toggle(Default, "Auto Quick 11", "Automatically performs the Quick 11 action for you.", false)
end

local GeneralTab = Window:Tab({Title = "General", Icon = "menu"})
local carTab    = Window:Tab({Title = "Car", Icon = "car"})
local MiscTab   = Window:Tab({Title = "MISC", Icon = "settings"})
local ServeeTab    = Window:Tab({Title = "Server", Icon = "server"})

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local PathfindingService = game:GetService("PathfindingService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local RootPart = Character:WaitForChild("HumanoidRootPart")

local Net = require(ReplicatedStorage.Modules.Core.Net)

LocalPlayer.CharacterAdded:Connect(function(Char)
    Character = Char
    Humanoid = Char:WaitForChild("Humanoid")
    RootPart = Char:WaitForChild("HumanoidRootPart")
end)

local AutoFarm = false
local FarmType = "ATM"
local ChipType = "Smart"
local ChipLimit = 5

local ChipLevels = {
    [1] = "HackToolBasic",
    [12] = "HackToolPro",
    [50] = "HackToolUltimate",
    [90] = "HackToolQuantum"
}

function GetSwiperLevel()
    local Gui = LocalPlayer.PlayerGui:FindFirstChild("Skills")
    if not Gui then return 1 end

    local Scroll = Gui.SkillsHolder.SkillsScrollingFrame

    for _,v in pairs(Scroll:GetChildren()) do
        if v.Name == "SkillOptionTemplate" then
            if string.find(v.SkillTitle.Text,"Swiper") then
                return tonumber(v.SkillTitle.Text:match("%d+")) or 1
            end
        end
    end

    return 1
end

function GetSmartChip()
    local Level = GetSwiperLevel()
    local Chip = "HackToolBasic"

    for Need,Name in pairs(ChipLevels) do
        if Level >= Need then
            Chip = Name
        end
    end

    return Chip
end

function SelectedChip()
    if ChipType == "Smart" then
        return GetSmartChip()
    end

    return ChipType
end

function GetVehicle()
    local Frame = LocalPlayer.PlayerGui.Items.ItemsHolder.ItemsScrollingFrame

    for _,v in pairs(Frame:GetChildren()) do
        if v:IsA("Frame") and v:GetAttribute("ItemType") then
            if string.lower(v:GetAttribute("ItemType")) == "car" then
                return v.ItemName.Text
            end
        end
    end
end

function GetItemInfo(Name)
    local Frame = LocalPlayer.PlayerGui.Items.ItemsHolder.ItemsScrollingFrame

    for _,v in pairs(Frame:GetChildren()) do
        if v:IsA("Frame") and v:FindFirstChild("ItemName") then
            if v.ItemName.Text == Name then
                return {
                    UID = v.Name,
                    Equipped = v.ItemEquipped.Visible,
                    Broken = v.DestroyedItemIcon.Visible
                }
            end
        end
    end
end

function EquipVehicle()
    local Name = GetVehicle()
    if not Name then return end

    local Info = GetItemInfo(Name)
    if not Info then return end

    if not Info.Equipped and not Info.Broken then
        Net.get("toggle_equip_item", tostring(Info.UID))
    end
end

function GetOwnedVehicle()
    for _,v in pairs(workspace.Vehicles:GetChildren()) do
        if v:GetAttribute("OwnerUserId") == LocalPlayer.UserId then
            return v
        end
    end
end

function SitVehicle()
    local Car = GetOwnedVehicle()
    if not Car or not Car.PrimaryPart then return end

    Car:PivotTo(RootPart.CFrame * CFrame.new(0,7,0))
    task.wait(0.3)

    for _,v in pairs(Car:GetDescendants()) do
        if v:IsA("ProximityPrompt") then
            fireproximityprompt(v)
        end
    end
end

function GetChipCount(Name)
    local Frame = LocalPlayer.PlayerGui.Items.ItemsHolder.ItemsScrollingFrame
    local Count = 0

    for _,v in pairs(Frame:GetChildren()) do
        if v:IsA("Frame") and v:FindFirstChild("ItemName") then
            if v.ItemName.Text == Name then
                if not v.DestroyedItemIcon.Visible then
                    Count += 1
                end
            end
        end
    end

    return Count
end
function IsATMBusy(ATM)
    if not ATM then
        return true
    end

    local Prompt = ATM:FindFirstChildWhichIsA("ProximityPrompt", true)

    if Prompt and not Prompt.Enabled then
        return true
    end

    return false
end

function EnsureVehicle()
    local Car = GetOwnedVehicle()

    if not Car then
        EquipVehicle()
        task.wait(1)
        SitVehicle()
        task.wait(1)
        return GetOwnedVehicle()
    end

    if not Car.PrimaryPart then
        EquipVehicle()
        task.wait(1)
        return GetOwnedVehicle()
    end

    return Car
end

function RecallVehicle()
    local Car = GetOwnedVehicle()

    if Car and Car.PrimaryPart then
        Car:PivotTo(RootPart.CFrame * CFrame.new(0,7,0))
        task.wait(0.5)
    else
        EquipVehicle()
        task.wait(1)
        SitVehicle()
    end
end

function GetClosestATM()
    local Closest = nil
    local Dist = math.huge

    for _,v in pairs(workspace.Map.Props.ATMs:GetChildren()) do
        if v.Name == "ATM" and not IsATMBusy(v) then
            local Mag = (v.Area.Position - RootPart.Position).Magnitude

            if Mag < Dist then
                Dist = Mag
                Closest = v
            end
        end
    end

    return Closest
end

local CarSpeed = 200

function DriveTo(Pos)
    local Car = EnsureVehicle()
    if not Car or not Car.PrimaryPart then return end

    local Path = PathfindingService:CreatePath({
        AgentRadius = 6,
        AgentHeight = 8,
        AgentCanJump = true
    })

    Path:ComputeAsync(Car.PrimaryPart.Position, Pos)

    local LastPos = Car.PrimaryPart.Position

    for _,Point in pairs(Path:GetWaypoints()) do
        local StartPos = Car.PrimaryPart.Position
        local Target = Point.Position + Vector3.new(0,4,0)

        local Direction = (Target - StartPos).Unit
        local Distance = (Target - StartPos).Magnitude

        local StartTime = tick()
        local Travel = 0

        while Travel < Distance do
            task.wait()

            local Passed = tick() - StartTime
            Travel = math.min(Passed * CarSpeed, Distance)

            local NewPos = StartPos + Direction * Travel

            Car:PivotTo(CFrame.lookAt(NewPos, NewPos + Direction))

            for _,Part in pairs(Car:GetDescendants()) do
                if Part:IsA("BasePart") then
                    Part.Velocity = Vector3.zero
                    Part.RotVelocity = Vector3.zero
                    Part.AssemblyLinearVelocity = Vector3.zero
                    Part.AssemblyAngularVelocity = Vector3.zero
                end
            end

            if (Car.PrimaryPart.Position - LastPos).Magnitude < 1 and Passed > 2 then
                RecallVehicle()
                return
            end

            LastPos = Car.PrimaryPart.Position
        end
    end
end

function RepairVehicle()
    Net.get("repair_vehicle")
end

function GetMoney()
    local Text = LocalPlayer.PlayerGui.TopRightHud.Holder.Frame.MoneyTextLabel.Text
    return tonumber(Text:match("%$(%d+)")) or 0
end

function DepositMoney()
    local Cash = GetMoney()

    if Cash > 0 then
        Net.get("transfer_funds","hand","bank",Cash)
    end
end

function BuyChip()
    local Chip = SelectedChip()
    local Shop = workspace.ShopZone_Illegal

    if Chip == "HackToolQuantum" then
        Shop = workspace.ShopZone_IllegalNightclub
    end

    for i = GetChipCount(Chip), ChipLimit - 1 do
        Net.get("purchase_consumable",Shop,Chip)
        task.wait(0.3)
    end
end

function HackATM()
    local ATM = GetClosestATM()
    if not ATM then return end

    local Car = GetOwnedVehicle()

    if Car and Car.PrimaryPart then
        DriveTo(ATM.Area.Position)
    end

    Net.get("exit_seat")
    task.wait(0.2)

    RootPart.CFrame = ATM.Area.CFrame * CFrame.new(0,0,3)
    task.wait(0.2)

    Net.get("request_begin_hacking_3",ATM,SelectedChip())
    task.wait(1)

    Net.get("atm_win_3",ATM)
end

function AutoFarmLoop()
    while AutoFarm do
        pcall(function()
            EquipVehicle()
            task.wait(1)

            if not Humanoid.Sit then
                SitVehicle()
            end

            local Chip = SelectedChip()

            if GetChipCount(Chip) <= 0 then
                DepositMoney()
                task.wait(0.5)
                BuyChip()
            end

            local Car = GetOwnedVehicle()

            if Car then
                local Info = GetItemInfo(GetVehicle())

                if Info and Info.Broken then
                    Car:PivotTo(RootPart.CFrame * CFrame.new(0,7,0))
                    task.wait(0.5)
                    RepairVehicle()
                end
            end

            HackATM()
        end)

        task.wait(1)
    end
end

GeneralTab:Dropdown({
    Title = "Farm Select",
    Description = "",
    Values = {"ATM"},
    Default = "ATM",
    Callback = function(v)
        FarmType = v
    end
})

GeneralTab:Toggle({
    Title = "Auto Farm",
    Desc = "",
    Value = false,
    Callback = function(v)
        AutoFarm = v

        if v then
            task.spawn(AutoFarmLoop)
        end
    end
})

GeneralTab:Dropdown({
    Title = "Chip Select",
    Description = "",
    Values = {
        "Smart",
        "HackToolBasic",
        "HackToolPro",
        "HackToolUltimate",
        "HackToolQuantum"
    },
    Default = "Smart",
    Callback = function(v)
        ChipType = v
    end
})

GeneralTab:Slider({
    Title = "Buy Chip Amount",
    Desc = "",
    Step = 1,
    Value = {
        Min = 5,
        Max = 15,
        Default = 5
    },
    Callback = function(v)
        ChipLimit = v
    end
})

GeneralTab:Slider({
    Title = "Car Speed",
    Desc = "",
    Step = 5,
    Value = {
        Min = 10,
        Max = 350,
        Default = 200
    },
    Callback = function(value)
        CarSpeed = value
    end
})